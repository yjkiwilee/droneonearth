// ---- resolution of the data ----

const RES = 1201; // resolution of the data
const RES_HALF = Math.floor(RES / 2); // floored half of the resolution
const RES_GEO = RES + 1; // resolution of the geometry

// ---- path to elevation data ----

const ELV_PATH = "elvdata/";